plugins {
    id 'java'
    id 'org.jetbrains.intellij.platform.module'
}

group = rootProject.group
version = rootProject.version

dependencies {
    intellijPlatform {
        intellijIdea("2026.1")
    }
}
